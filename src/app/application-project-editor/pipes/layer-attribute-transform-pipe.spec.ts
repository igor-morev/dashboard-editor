import { LayerAttributeTransformPipe } from './layer-attribute-transform-pipe';

describe('LayerAttributeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new LayerAttributeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
