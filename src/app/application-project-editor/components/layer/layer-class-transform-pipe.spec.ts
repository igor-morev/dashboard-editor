import { LayerClassTransformPipe } from './layer-class-transform-pipe';

describe('LayerClassTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new LayerClassTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
